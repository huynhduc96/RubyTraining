<%= link_to gravatar_for(current_user, size: Settings.length_name), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("users.info.view_pro"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t("users.info.micropost") %></span>
